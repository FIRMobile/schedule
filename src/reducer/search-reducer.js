export default function search(
    state = {
      isLoggedIn: false,
      isReadyBot: false,
    },
    action = {},
  ) {
    switch (action.type) {
      default:
        return state;
    }
  }
  